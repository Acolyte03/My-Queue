<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <title>My Queue</title>
</head>
<body>
     <p>Note: you are viewing homepage.handlebars</p>
<!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                {{#if loggedIn}}
                    <li class="nav-item">
                        <a class="nav-link" href="/watchlist">Watchlist</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/browse">Browse</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">Logout</a>
                    </li>
                {{else}}
                  
                {{/if}}
            </ul>
        </div>
    </div>
</nav>

<!-- Hero Section -->
<header class="py-5 bg-dark text-white">
    <div class="container h-100">
        <div class="row h-100 align-items-center">
            <div class="col-lg-7">
                <h1 class="display-4">Never miss a show with My Queue!</h1>
                <p class="lead">Your personal watchlist companion. Find. Add. Watch. Repeat.</p>
                {{#unless loggedIn}}
                <a href="/login" class="btn btn-primary btn-lg">Sign Up Now</a>
                {{/unless}}
            </div>
        </div>
    </div>
</header>

<!-- Main Content -->
<main role="main" class="container mt-4">
    <section class="text-center my-5">
        <h2 class="fw-bold">Featured TV Shows</h2>
        <div class="row">
            <!-- Placeholder for featured TV shows -->
            <!-- This would be dynamically generated with backend logic -->
        </div>
    </section>

    <section class="my-5">
        <h2 class="fw-bold text-center">How It Works</h2>
        <div class="row text-center">
            <div class="col-md-4">
                <span class="fa-stack fa-4x">
                    <i class="fas fa-circle fa-stack-2x text-primary"></i>
                    <i class="fas fa-search fa-stack-1x fa-inverse"></i>
                </span>
                <h4 class="my-3">Find Shows</h4>
                <p>Search from a vast library of TV shows and find the ones you love.</p>
            </div>
            <div class="col-md-4">
                <span class="fa-stack fa-4x">
                    <i class="fas fa-circle fa-stack-2x text-primary"></i>
                    <i class="fas fa-list fa-stack-1x fa-inverse"></i>
                </span>
                <h4 class="my-3">Add to Watchlist</h4>
                <p>Add any TV show to your personal watchlist with just a click.</p>
            </div>
            <div class="col-md-4">
                <span class="fa-stack fa-4x">
                    <i class="fas fa-circle fa-stack-2x text-primary"></i>
                    <i class="fas fa-tv fa-stack-1x fa-inverse"></i>
                </span>
                <h4 class="my-3">Watch & Enjoy</h4>
                <p>Keep track of what to watch next and enjoy your TV time.</p>
            </div>
        </div>
    </section>
    {{{body}}}
</main>

{{#if loggedIn}}
      <script src="/js/logout.js"></script>
{{/if}}

<!-- Bootstrap -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
<!-- Font Awesome for Icons (if you need icons) -->
<script src="https://kit.fontawesome.com/your-fontawesome-kit.js"></script>
</body>
</html>