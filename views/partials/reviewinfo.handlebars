<div class="card">
    <div class="card-header">
        <h4><a class="link" href="/reviews/edit/{{id}}">{{title}}</a></h4>
        <p class ="review-comment">{{comment}}</p>
    </div>
</div>

{{!-- {{#if reviews.length}}
<section>
  <h2>Your Reviews</h2>
    {{#each reviews as |review|}}
      {{> reviewinfo review}}
    {{/each}}

</section>
{{/if}} --}}